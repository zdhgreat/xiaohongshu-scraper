# 小红书搜索风控 (-104) 分析报告

> 日期：2026-05-27 | 分析对象：xiaohongshu_scraper_skill

---

## 一、问题现状

### 1.1 现象

小红书搜索 API（`POST /api/sns/web/v1/search/notes`）返回业务错误码 `-104`，错误消息为"您当前登录的账号没有权限访问"。

- HTTP 状态码：200（业务错误，不是网络层拦截）
- 响应时间：24-40ms（服务端瞬间拒绝）
- 影响范围：**所有账号**（default、account3 等）
- 发生时间：2026-05-21 开始间歇性出现，05-27 完全封锁

### 1.2 关键证据：不是 IP 封锁

从 `runs.jsonl` 日志中发现同一 session 内的交替模式：

```
03:03:36  homefeed  → code 0     ✅ 成功
03:03:47  search    → code -104  ❌ 被拒（11秒后）
03:04:34  homefeed  → code 0     ✅ 成功
03:04:50  search    → code -104  ❌ 被拒
03:09:06  homefeed  → code 0     ✅ 成功
03:09:16  search    → code -104  ❌ 被拒
```

**同一 IP、同一账号、同一 session**：homefeed 正常，search 被拒。

### 1.3 手动验证

在真实 Chrome 浏览器中，使用**同一 IP、同一账号**手动搜索 → **搜索正常**。

### 1.4 结论

| 排除项 | 证据 |
|--------|------|
| ❌ 不是 IP 封锁 | 同 IP 手动浏览器搜索正常 |
| ❌ 不是账号限制 | 同账号 homefeed 正常，手动搜索正常 |
| ✅ 是 API 请求检测 | 服务端能区分 HTTP API 调用和真实浏览器操作 |

**-104 的本质：小红书搜索 API 检测到请求不是来自真实浏览器操作，拒绝了 API 层面的搜索调用。**

---

## 二、当前爬虫 vs 真实浏览器的差距

### 2.1 当前爬虫的请求方式

```
CLI 命令
  → curl_cffi HTTP session（TLS 指纹模拟 Chrome 131）
  → JS 签名引擎生成 x-s / x-t / x-s-common
  → 直接 POST https://edith.xiaohongshu.com/api/sns/web/v1/search/notes
  → 服务端返回 -104
```

虽然做了 TLS 指纹模拟、完整请求头、JS 签名，但缺少：

| 维度 | 真实浏览器 | 当前爬虫 (HTTP) |
|------|-----------|----------------|
| JS 执行环境 | 完整 V8 引擎 + DOM | 无（只有签名计算） |
| Canvas/WebGL 指纹 | 真实渲染结果 | 无 |
| 浏览器行为 | 页面导航→输入搜索→等待加载 | 直接调 API |
| 请求来源 | 页面内 JS 发起（有 Referer 链） | 外部 HTTP 客户端 |
| Cookie 生态 | 完整（含 JS 设置的动态 cookie） | 只有静态 cookie |
| 搜索操作路径 | `/search?keyword=xxx` 页面交互 | `/api/sns/web/v1/search/notes` API 直调 |

### 2.2 为什么 homefeed 不被检测

homefeed 是首页默认加载，几乎所有正常访问都会触发。搜索则是主动行为，小红书对搜索接口的风控明显更严格。这是合理的 — 搜索是数据泄露风险最高的接口。

---

## 三、MediaCrawler 分析

### 3.1 MediaCrawler 是什么

开源多平台社媒爬虫（GitHub 49.5k 星），支持小红书、抖音、B站等。

核心架构：**Playwright/CDP 浏览器自动化 + aiohttp HTTP 请求**

### 3.2 MediaCrawler 能否解决 -104

**不能。** 原因：

1. **MediaCrawler 也遇到 -104** — GitHub Issues #754、#769、#865 大量用户反馈搜索被封锁
2. **MediaCrawler 的搜索仍走 API** — CDP 提取签名后用 aiohttp 发 HTTP 请求，不是真正的页面 DOM 操作
3. **Pro 版把多账号+代理池作为付费卖点** — 说明开源版没有技术手段绕过搜索封锁

### 3.3 功能对比

| 维度 | 当前爬虫 | MediaCrawler 开源版 |
|------|---------|-------------------|
| 搜索 API | ❌ -104 | ❌ 同样 -104 |
| feed/推荐流 | ✅ 正常 | ✅ 正常 |
| 爬指定用户 | ✅ crawl-user | ✅ creator 模式 |
| 多账号轮换 | ✅ LRU+冷却 | ❌ 仅 Pro 版 |
| 代理池 | ✅ 指数冷却 | ⚠️ 仅2个供应商 |
| 断点续爬 | ✅ 已实现 | ❌ 仅 Pro 版 |
| 日志/监控 | ✅ JSONL+PostgreSQL | 基础 CSV/DB |
| 媒体分析 | ✅ OCR+AI+Mermaid | ❌ 无 |
| 反风控措施 | 24 项 | 基础（浏览器指纹+随机间隔） |

**结论：当前爬虫功能已全面超越 MediaCrawler 开源版，没有必要切换。**

---

## 四、不依赖代理池的解决方案

### 4.1 核心思路

**搜索走浏览器 DOM 操作，数据获取走 API。**

既然手动浏览器搜索正常，那就让爬虫也用浏览器方式搜索。搜索是最敏感的环节，模拟真人操作；获取笔记详情是正常操作，API 就行。

### 4.2 三层防御方案

#### 第 1 层：搜索专用速度配置

当前问题：搜索和浏览用同样的速度配置（normal: 3-6 连发），对搜索来说太激进。

新增 `search` 专用 SpeedProfile：

```
搜索不 burst：每次只搜 1 个关键词
搜索间隔：30-60s（人工搜索的节奏）
搜索间休息：5-10 分钟（模拟看搜索结果）
每 5 次搜索：强制休息 30-60 分钟
```

#### 第 2 层：搜索专用 warmup

当前问题：warmup 只调 homefeed，搜索前没有行为模拟。

搜索前额外做：
1. 先调 homefeed 模拟打开首页
2. 等待 5-15s 模拟浏览
3. 然后才发搜索请求

#### 第 3 层：-104 自动降级到浏览器 DOM 搜索

当前问题：-104 没有恢复逻辑，直接返回空数据。

**新增 `PlaywrightDomSearch` 类**，与现有 `PlaywrightTakeover` 的区别：

| | PlaywrightTakeover（现有） | PlaywrightDomSearch（新增） |
|---|---|---|
| 方式 | 在浏览器里调 `fetch()` API | 在浏览器里导航搜索页面 |
| 流程 | browser → fetch(api) → 解析 JSON | browser → 输入关键词 → 等加载 → 提取 DOM |
| 是否模拟真人 | ❌ 仍然是 API 调用 | ✅ 完全模拟真人搜索操作 |

DOM 搜索流程：
```
检测到 -104
  → 启动 PlaywrightDomSearch
  → 打开 xiaohongshu.com/explore
  → 在搜索框输入关键词（模拟打字速度）
  → 等待搜索结果加载
  → 从 DOM 提取笔记列表（标题、note_id、封面等）
  → 返回结果（note_id 可走正常 API 获取详情）
```

### 4.3 频率调整对比

| 参数 | 当前 | 搜索专用 | 说明 |
|------|------|---------|------|
| burst 大小 | 3-6 | 1 | 搜索不连发 |
| 搜索间隔 | 2.5-5.5s | 30-60s | 人工搜索节奏 |
| 搜索间休息 | 20-60s | 5-10min | 给服务器"消化" |
| 强休频率 | 每 50 次 | 每 5 次 | 搜索强度远高于浏览 |
| 强休时长 | 5-10min | 30-60min | 充分冷却 |

### 4.4 改动范围

| 文件 | 改动内容 | 预计行数 |
|------|---------|---------|
| `xhs_config.py` | 新增 search SpeedProfile + 搜索 IP 限速参数 | ~10 行 |
| `xhs_fetcher.py` | 搜索识别 + 搜索 warmup + -104 处理 + PlaywrightDomSearch | ~100 行 |
| `xhs_api.py` | search 函数支持浏览器搜索 | ~15 行 |

**总计约 125 行，不创建新文件，不引入新依赖。**

---

## 五、预期效果

| 场景 | 当前 | 方案实施后 |
|------|------|-----------|
| API 搜索 | ❌ -104 被拒 | 自动降级到浏览器 DOM 搜索 ✅ |
| 连续搜索 | ❌ 全部 -104 | 30-60s 间隔 + 每 5 次休息 30min ✅ |
| feed/推荐流 | ✅ 正常 | 不受影响 |
| crawl-user | ✅ 正常 | 不受影响 |
| 依赖代理 | 不需要 | 仍然不需要 |

---

## 六、风险和限制

1. **浏览器 DOM 搜索速度慢** — 每次搜索需要 10-30s（启动浏览器 + 页面加载 + DOM 提取），远慢于 API 调用
2. **Playwright 有自动化特征** — `navigator.webdriver` 等标记可能被检测，需要配合 stealth 措施
3. **小红书页面结构可能变化** — DOM 选择器需要维护
4. **搜索结果数量有限** — DOM 搜索只能获取第一页结果（通常 20 条），翻页需要额外处理
5. **浏览器资源消耗** — Playwright 启动 Chromium 占用内存约 200-500MB

---

## 七、建议优先级

```
推荐实施顺序：
  1. 第 3 层（-104 降级到 DOM 搜索）— 直接解决当前问题，最紧急
  2. 第 1 层（搜索专用速度配置）— 长期稳定性的基础
  3. 第 2 层（搜索 warmup）— 锦上添花
```

---

*报告日期：2026-05-27*
*分析基于：runs.jsonl 日志、xhs_fetcher.py 源码、MediaCrawler GitHub 研究结果*
